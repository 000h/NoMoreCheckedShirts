<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="kr.co.nmcs.account">

	<select id="accountAll" resultType="adto">
		SELECT * FROM account
	</select>

	<select id="register" parameterType="adto">
		INSERT INTO account(acode,
		id, pw, name, hp, addrs, postcode, astatus)
		VALUES (account_seq, #{id},
		#{pw}, #{name}, #{hp}, #{addrs}, #{postcode}, 0)
	</select>


	<select id="modifyAccount" parameterType="adto">
		UPDATE account
		SET pw=#{pw}, #{name}, #{hp}, #{addrs}, #{postcode}, #{astatus}
		WEHRE acode=#{acode}
	</select>

	<select id="withdrawal">
		DELETE account
		WHERE acode=#{acode}
	</select>


</mapper>